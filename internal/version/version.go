// Package version holds the releasejo build version. The literal is bumped by
// releasejo itself via the x-release-please-version marker (dogfooding the
// generic updater — see release-please-config.json extra-files).
package version

const Version = "0.0.0" // x-release-please-version
