// releasejo — release-please-compatible release automation for Forgejo & Gitea.
// Stdlib-only by design: a single static binary, trivial to audit and to run
// air-gapped inside CI. No third-party dependencies.
module github.com/lassediercksAI/releasejo

go 1.26
